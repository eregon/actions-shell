name: Shell
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@master
    # - uses: ruby/setup-ruby@v1
    #   with:
    #     ruby-version: 3.2
    - name: Setup SSH session
      uses: lhotari/action-upterm@v1
      with:
        limit-access-to-users: eregon
    - run: ruby test.rb
